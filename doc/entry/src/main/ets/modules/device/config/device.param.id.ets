/**
 * 日期和时间
 * 用于设置和读取，每次连接后需要同步泵端的时间
 */
export const PARAM_ID_TIMESTAMP = 5;

/**
 * 报告机器类型和时间, 注意控制设备连接蓝牙之后，需要立马发送该命令，泵会回送该命令，以确定时间是否要更改，
 * 主要是手环那边时间不准，以泵的这条命令来同步时间(不允许查询)
 */
export const PARAM_ID_REPORT_DEVICE = 17;
